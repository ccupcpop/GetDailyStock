name: 每日台股分析-Gmail通知

on:
  schedule:
    # 每天 UTC 09:15 執行 = 台灣時間下午 17:15 (UTC+8)
    - cron: '30 8 * * *'
  
  workflow_dispatch:

jobs:
  stock-analysis:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk chromium-browser chromium-chromedriver
      
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Ensure data directories exist
        run: |
          mkdir -p StockHTML StockPNG StockOTCHTML StockOTCPNG
      
      - name: Run stock analysis
        env:
          TZ: 'Asia/Taipei'
        run: |
          python 上市上櫃全流程.py
      
      - name: Package analysis results
        run: |
          # 創建日期標記
          DATE=$(TZ=Asia/Taipei date +'%Y%m%d')
          DATETIME=$(TZ=Asia/Taipei date +'%Y-%m-%d %H:%M')
          
          # 打包所有分析結果
          mkdir -p release_package
          
          # 複製 Excel 報表
          cp -f *.xlsx release_package/ 2>/dev/null || true
          
          # 複製 HTML 和 PNG 圖表
          cp -f StockHTML/*.html release_package/ 2>/dev/null || true
          cp -f StockPNG/*.png release_package/ 2>/dev/null || true
          cp -f StockOTCHTML/*.html release_package/ 2>/dev/null || true
          cp -f StockOTCPNG/*.png release_package/ 2>/dev/null || true
          
          # 創建壓縮檔
          cd release_package
          zip -r ../stock-analysis-${DATE}.zip . -x "*.git*"
          cd ..
          
          echo "DATE=${DATE}" >> $GITHUB_ENV
          echo "DATETIME=${DATETIME}" >> $GITHUB_ENV
          
          # 顯示打包內容
          echo "打包內容:"
          unzip -l stock-analysis-${DATE}.zip | head -20
      
      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: analysis-${{ env.DATE }}
          name: 股票分析報告 ${{ env.DATETIME }}
          body: |
            ## 📊 台股分析報告
            
            **分析時間:** ${{ env.DATETIME }}
            
            ### 📦 下載內容
            - ✅ 上市分析報表 (analysis_result.xlsx)
            - ✅ 上櫃分析報表 (otc_analysis_result.xlsx)
            - ✅ 所有 HTML 互動圖表
            - ✅ 所有 PNG 靜態圖表
            
            ### 📈 分析指標
            - 成交量異常分析
            - 法人買賣超統計
            - 漲跌幅排行
            - 技術指標分析
            
            ---
            
            ⚠️ 此 release 會在 30 天後自動刪除
          files: |
            stock-analysis-${{ env.DATE }}.zip
            analysis_result.xlsx
            otc_analysis_result.xlsx
          draft: false
          prerelease: false
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Get Release URL
        run: |
          REPO="${{ github.repository }}"
          TAG="analysis-${{ env.DATE }}"
          RELEASE_URL="https://github.com/${REPO}/releases/tag/${TAG}"
          echo "RELEASE_URL=${RELEASE_URL}" >> $GITHUB_ENV
          echo "📎 Release URL: ${RELEASE_URL}"
      
      - name: Remove source code archives from release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: analysis-${{ env.DATE }}
        run: |
          # 等待 release 完全創建
          sleep 5
          
          # 使用 GitHub API 刪除 source code 壓縮檔
          REPO="${{ github.repository }}"
          
          # 獲取 release ID
          RELEASE_ID=$(curl -s -H "Authorization: token $GITHUB_TOKEN" \
            "https://api.github.com/repos/${REPO}/releases/tags/${TAG}" | \
            jq -r '.id')
          
          if [ "$RELEASE_ID" != "null" ] && [ -n "$RELEASE_ID" ]; then
            # 獲取所有 assets
            ASSETS=$(curl -s -H "Authorization: token $GITHUB_TOKEN" \
              "https://api.github.com/repos/${REPO}/releases/${RELEASE_ID}/assets")
            
            # 刪除 source code 壓縮檔
            echo "$ASSETS" | jq -r '.[] | select(.name | test("^(Source code|源代碼).*\\.(zip|tar\\.gz)$")) | .id' | while read asset_id; do
              if [ -n "$asset_id" ]; then
                echo "🗑️  刪除 source code asset: $asset_id"
                curl -X DELETE -H "Authorization: token $GITHUB_TOKEN" \
                  "https://api.github.com/repos/${REPO}/releases/assets/${asset_id}"
              fi
            done
            
            echo "✅ 已移除 source code 壓縮檔"
          else
            echo "⚠️  找不到 release ID"
          fi
      
      - name: Send Email with Attachments
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_PASSWORD: ${{ secrets.GMAIL_PASSWORD }}
          RECIPIENT_EMAIL: ${{ secrets.RECIPIENT_EMAIL }}
        run: |
          # 安裝 Python 郵件套件和資料處理套件
          pip install secure-smtplib pandas openpyxl
          
          # 創建發送郵件的 Python 腳本
          cat > send_email.py << 'EOF'
          import smtplib
          import os
          import pandas as pd
          from email.mime.multipart import MIMEMultipart
          from email.mime.text import MIMEText
          from email.mime.base import MIMEBase
          from email import encoders
          from datetime import datetime
          
          def read_excel_data():
              """讀取 Excel 檔案並提取關鍵統計資料"""
              tables_html = ""
              
              try:
                  # 讀取上市股票分析報表
                  if os.path.exists('analysis_result.xlsx'):
                      df = pd.read_excel('analysis_result.xlsx', sheet_name='成交量異常', nrows=10)
                      tables_html += """
                      <h3 style="color: #d32f2f; margin-top: 30px;">🔴 上市 - 成交量異常 TOP 10</h3>
                      """ + df.to_html(index=False, classes='stock-table', border=0, justify='left')
                      
                      # 讀取漲幅前10
                      df_up = pd.read_excel('analysis_result.xlsx', sheet_name='漲幅前10', nrows=10)
                      tables_html += """
                      <h3 style="color: #388e3c; margin-top: 30px;">📈 上市 - 漲幅前 10</h3>
                      """ + df_up.to_html(index=False, classes='stock-table', border=0, justify='left')
                      
                      # 讀取跌幅前10
                      df_down = pd.read_excel('analysis_result.xlsx', sheet_name='跌幅前10', nrows=10)
                      tables_html += """
                      <h3 style="color: #f57c00; margin-top: 30px;">📉 上市 - 跌幅前 10</h3>
                      """ + df_down.to_html(index=False, classes='stock-table', border=0, justify='left')
                  
                  # 讀取上櫃股票分析報表
                  if os.path.exists('otc_analysis_result.xlsx'):
                      df_otc = pd.read_excel('otc_analysis_result.xlsx', sheet_name='成交量異常', nrows=10)
                      tables_html += """
                      <h3 style="color: #d32f2f; margin-top: 30px;">🔴 上櫃 - 成交量異常 TOP 10</h3>
                      """ + df_otc.to_html(index=False, classes='stock-table', border=0, justify='left')
                      
                      # 讀取上櫃漲幅前10
                      df_otc_up = pd.read_excel('otc_analysis_result.xlsx', sheet_name='漲幅前10', nrows=10)
                      tables_html += """
                      <h3 style="color: #388e3c; margin-top: 30px;">📈 上櫃 - 漲幅前 10</h3>
                      """ + df_otc_up.to_html(index=False, classes='stock-table', border=0, justify='left')
                      
              except Exception as e:
                  print(f'⚠️  讀取 Excel 資料時發生錯誤: {str(e)}')
                  tables_html = "<p style='color: #f57c00;'>⚠️ 無法讀取統計資料</p>"
              
              return tables_html
          
          def send_email():
              # 從環境變數讀取設定
              sender_email = os.environ.get('GMAIL_USER')
              sender_password = os.environ.get('GMAIL_PASSWORD')
              recipient_email = os.environ.get('RECIPIENT_EMAIL')
              release_url = os.environ.get('RELEASE_URL')
              date_str = os.environ.get('DATE')
              datetime_str = os.environ.get('DATETIME')
              
              # 讀取統計資料
              statistics_tables = read_excel_data()
              
              # 創建郵件
              msg = MIMEMultipart()
              msg['From'] = sender_email
              msg['To'] = recipient_email
              msg['Subject'] = f'📊 台股分析報告 {datetime_str}'
              
              # 郵件內容
              body = f"""
          <html>
            <head>
              <style>
                body {{ 
                  font-family: 'Microsoft JhengHei', Arial, sans-serif;
                  line-height: 1.6;
                  color: #333;
                }}
                .download-section {{
                  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                  padding: 30px;
                  border-radius: 12px;
                  margin: 20px 0;
                  text-align: center;
                }}
                .download-title {{
                  color: white;
                  font-size: 20px;
                  font-weight: bold;
                  margin-bottom: 15px;
                }}
                .download-btn {{
                  display: inline-block;
                  padding: 15px 40px;
                  background-color: white;
                  color: #667eea;
                  text-decoration: none;
                  border-radius: 8px;
                  font-size: 16px;
                  font-weight: bold;
                  box-shadow: 0 4px 15px rgba(0,0,0,0.2);
                  transition: transform 0.2s;
                }}
                .download-btn:hover {{
                  transform: translateY(-2px);
                  box-shadow: 0 6px 20px rgba(0,0,0,0.3);
                }}
                .stock-table {{
                  border-collapse: collapse;
                  width: 100%;
                  margin: 15px 0;
                  font-size: 13px;
                }}
                .stock-table th {{
                  background-color: #f5f5f5;
                  padding: 10px 8px;
                  text-align: left;
                  font-weight: bold;
                  border-bottom: 2px solid #ddd;
                }}
                .stock-table td {{
                  padding: 8px;
                  border-bottom: 1px solid #eee;
                }}
                .stock-table tr:hover {{
                  background-color: #f9f9f9;
                }}
                .section-title {{
                  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                  color: white;
                  padding: 15px 20px;
                  border-radius: 8px;
                  margin-top: 30px;
                  font-size: 18px;
                }}
              </style>
            </head>
            <body>
              <h2 style="color: #667eea;">📊 台股每日分析報告</h2>
              
              <p><strong>分析時間:</strong> {datetime_str}</p>
              
              <div class="download-section">
                <div class="download-title">📦 完整報告下載</div>
                <a href="{release_url}" class="download-btn">
                  🔗 下載完整分析包 (含所有圖表)
                </a>
                <p style="color: white; margin-top: 15px; font-size: 14px; opacity: 0.9;">
                  包含: Excel 報表 • HTML 互動圖表 • PNG 靜態圖表
                </p>
              </div>
              
              <hr style="margin: 30px 0; border: none; border-top: 2px solid #ddd;">
              
              {statistics_tables}
              
              <hr style="margin: 30px 0; border: none; border-top: 1px solid #ddd;">
              
              <div class="section-title">📎 附件檔案</div>
              <ul style="margin-top: 15px;">
                <li style="margin: 8px 0;">✅ <strong>analysis_result.xlsx</strong> - 上市股票分析報表（完整資料）</li>
                <li style="margin: 8px 0;">✅ <strong>otc_analysis_result.xlsx</strong> - 上櫃股票分析報表（完整資料）</li>
              </ul>
              
              <p style="margin-top: 30px; color: #666; font-size: 12px; text-align: center; padding-top: 20px; border-top: 1px solid #eee;">
                🤖 此報告由 GitHub Actions 自動產生<br>
                📅 生成時間: {datetime_str}
              </p>
            </body>
          </html>
          """
              
              msg.attach(MIMEText(body, 'html', 'utf-8'))
              
              # 附加 Excel 檔案
              files_to_attach = [
                  'analysis_result.xlsx',
                  'otc_analysis_result.xlsx'
              ]
              
              for filename in files_to_attach:
                  if os.path.exists(filename):
                      with open(filename, 'rb') as f:
                          part = MIMEBase('application', 'octet-stream')
                          part.set_payload(f.read())
                          encoders.encode_base64(part)
                          part.add_header(
                              'Content-Disposition',
                              f'attachment; filename= {filename}'
                          )
                          msg.attach(part)
                          print(f'✅ 附加檔案: {filename}')
                  else:
                      print(f'⚠️  檔案不存在: {filename}')
              
              # 發送郵件
              try:
                  server = smtplib.SMTP('smtp.gmail.com', 587)
                  server.starttls()
                  server.login(sender_email, sender_password)
                  server.send_message(msg)
                  server.quit()
                  print(f'✅ 郵件已成功發送到: {recipient_email}')
              except Exception as e:
                  print(f'❌ 郵件發送失敗: {str(e)}')
                  raise
          
          if __name__ == '__main__':
              send_email()
          EOF
          
          # 執行發送郵件
          python send_email.py
      
      - name: Commit stock data to repo
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          git add -A StockDaily/*.csv \
                     StockShares/*.csv \
                     StockOTCDaily/*.csv \
                     StockOTCShares/*.csv \
                     StockHistory/*.csv \
                     StockOTCHistory/*.csv 2>/dev/null || true
          
          if git diff --staged --quiet; then
            echo "❌ 沒有資料變更"
          else
            git commit -m "📊 更新股票資料 $(TZ=Asia/Taipei date +'%Y-%m-%d %H:%M')"
            git push
            echo "✅ 成功推送資料到 repository"
          fi
      
      - name: Clean old releases (keep last 30 days)
        uses: dev-drprasad/delete-older-releases@v0.3.2
        with:
          keep_latest: 30
          delete_tags: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}