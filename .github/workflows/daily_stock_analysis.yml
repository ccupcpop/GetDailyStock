name: 每日台股分析-Gmail通知

on:
  schedule:
    # 每天 UTC 09:15 執行 = 台灣時間下午 17:15 (UTC+8)
    - cron: '0 8 * * *'
  
  workflow_dispatch:

jobs:
  stock-analysis:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk chromium-browser chromium-chromedriver
      
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Ensure data directories exist
        run: |
          mkdir -p StockTSEHTML StockOTCHTML StockInfo
      
      - name: Run stock analysis
        env:
          TZ: 'Asia/Taipei'
          DEBUG_MODE: 'false'  # 改成 'true' 啟用除錯模式 (跳過爬蟲和 History)
        run: |
          if [ "$DEBUG_MODE" = "true" ]; then
            echo "⚡ 除錯模式：跳過爬蟲和 History 生成"
            python 上市上櫃全流程.py --debug-skip-data-processing --copy-to-repo
          else
            echo "🚀 完整流程模式"
            python 上市上櫃全流程.py --copy-to-repo
          fi
      
      - name: Verify files in StockInfo directory
        run: |
          echo "📦 檢查 StockInfo 資料夾的檔案..."
          
          # 取得台灣時間日期 (格式: YYYYMMDD)
          DATE=$(TZ=Asia/Taipei date +'%Y%m%d')
          echo "📅 台灣時間日期: ${DATE}"
          echo ""
          
          # 檢查 StockInfo 資料夾
          if [ -d "StockInfo" ]; then
            echo "📋 StockInfo 資料夾內容:"
            ls -lh StockInfo/
            echo ""
            
            # 檢查帶日期的檔案是否存在
            echo "🔍 檢查帶日期的檔案:"
            [ -f "StockInfo/tse_analysis_result_${DATE}.xlsx" ] && echo "✅ tse_analysis_result_${DATE}.xlsx" || echo "⚠️  tse_analysis_result_${DATE}.xlsx 不存在"
            [ -f "StockInfo/otc_analysis_result_${DATE}.xlsx" ] && echo "✅ otc_analysis_result_${DATE}.xlsx" || echo "⚠️  otc_analysis_result_${DATE}.xlsx 不存在"
            [ -f "StockInfo/ALL_TSE_${DATE}.html" ] && echo "✅ ALL_TSE_${DATE}.html" || echo "⚠️  ALL_TSE_${DATE}.html 不存在"
            [ -f "StockInfo/ALL_OTC_${DATE}.html" ] && echo "✅ ALL_OTC_${DATE}.html" || echo "⚠️  ALL_OTC_${DATE}.html 不存在"
          else
            echo "❌ StockInfo 資料夾不存在！"
          fi
      
      - name: Package analysis results
        run: |
          # 創建日期標記
          DATE=$(TZ=Asia/Taipei date +'%Y%m%d')
          DATETIME=$(TZ=Asia/Taipei date +'%Y-%m-%d %H:%M')
          
          # 打包所有分析結果
          mkdir -p release_package
          
          # 複製 Excel 報表
          cp -f *.xlsx release_package/ 2>/dev/null || true
          
          # 複製 HTML 和 PNG 圖表
          cp -f StockTSEHTML/*.html release_package/ 2>/dev/null || true
          cp -f StockOTCHTML/*.html release_package/ 2>/dev/null || true
          
          # 創建壓縮檔
          cd release_package
          zip -r ../stock-analysis-${DATE}.zip . -x "*.git*"
          cd ..
          
          echo "DATE=${DATE}" >> $GITHUB_ENV
          echo "DATETIME=${DATETIME}" >> $GITHUB_ENV
          
          # 顯示打包內容
          echo "打包內容:"
          unzip -l stock-analysis-${DATE}.zip | head -20
      
      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: analysis-${{ env.DATE }}
          name: 股票分析報告 ${{ env.DATETIME }}
          body: |
            ## 📊 台股分析報告
            
            **分析時間:** ${{ env.DATETIME }}
            
            ### 📦 下載內容
            - ✅ 上市分析報表 (tse_analysis_result.xlsx)
            - ✅ 上櫃分析報表 (otc_analysis_result.xlsx)
            - ✅ 所有 HTML 互動圖表
            - ✅ 所有 PNG 靜態圖表
            
            ### 📈 分析指標
            - 成交量異常分析
            - 法人買賣超統計
            - 漲跌幅排行
            - 技術指標分析
            
            ---
            
            ⚠️ 此 release 會在 30 天後自動刪除
          files: |
            tse_analysis_result.xlsx
            otc_analysis_result.xlsx
          draft: false
          prerelease: false
          generate_release_notes: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Get Release URL
        run: |
          REPO="${{ github.repository }}"
          TAG="analysis-${{ env.DATE }}"
          RELEASE_URL="https://github.com/${REPO}/releases/tag/${TAG}"
          echo "RELEASE_URL=${RELEASE_URL}" >> $GITHUB_ENV
          echo "📎 Release URL: ${RELEASE_URL}"
      
      - name: Remove source code archives from release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: analysis-${{ env.DATE }}
        run: |
          # 等待 release 完全創建
          sleep 5
          
          # 使用 GitHub API 刪除 source code 壓縮檔
          REPO="${{ github.repository }}"
          
          # 獲取 release ID
          RELEASE_ID=$(curl -s -H "Authorization: token $GITHUB_TOKEN" \
            "https://api.github.com/repos/${REPO}/releases/tags/${TAG}" | \
            jq -r '.id')
          
          if [ "$RELEASE_ID" != "null" ] && [ -n "$RELEASE_ID" ]; then
            # 獲取所有 assets
            ASSETS=$(curl -s -H "Authorization: token $GITHUB_TOKEN" \
              "https://api.github.com/repos/${REPO}/releases/${RELEASE_ID}/assets")
            
            # 刪除 source code 壓縮檔
            echo "$ASSETS" | jq -r '.[] | select(.name | test("^(Source code|源代碼).*\\.(zip|tar\\.gz)$")) | .id' | while read asset_id; do
              if [ -n "$asset_id" ]; then
                echo "🗑️  刪除 source code asset: $asset_id"
                curl -X DELETE -H "Authorization: token $GITHUB_TOKEN" \
                  "https://api.github.com/repos/${REPO}/releases/assets/${asset_id}"
              fi
            done
            
            echo "✅ 已移除 source code 壓縮檔"
          else
            echo "⚠️  找不到 release ID"
          fi
      
      - name: Send Email with Attachments
        env:
          GMAIL_USER: ${{ secrets.GMAIL_USER }}
          GMAIL_PASSWORD: ${{ secrets.GMAIL_PASSWORD }}
          RECIPIENT_EMAIL: ${{ secrets.RECIPIENT_EMAIL }}
        run: |
          # 安裝 Python 郵件套件
          pip install secure-smtplib
          
          # 創建發送郵件的 Python 腳本
          cat > send_email.py << 'EOF'
          import smtplib
          import os
          from email.mime.multipart import MIMEMultipart
          from email.mime.text import MIMEText
          from email.mime.base import MIMEBase
          from email import encoders
          
          def send_email():
              sender_email = os.environ.get('GMAIL_USER')
              sender_password = os.environ.get('GMAIL_PASSWORD')
              recipient_email_primary = os.environ.get('RECIPIENT_EMAIL')
              release_url = os.environ.get('RELEASE_URL')
              date_str = os.environ.get('DATE')
              datetime_str = os.environ.get('DATETIME')
              
              # 多個收件人
              recipient_emails = [recipient_email_primary, 'i07250717@gmail.com', 'hospital691202@yahoo.com.tw']
              recipients_str = ', '.join(recipient_emails)
              
              # 創建郵件
              msg = MIMEMultipart()
              msg['From'] = sender_email
              msg['To'] = recipients_str
              msg['Subject'] = f'📊 台股分析報告 {datetime_str}'
              
              # 郵件內容
              body = f"""
          <html>
            <head>
              <style>
                body {{ 
                  font-family: 'Microsoft JhengHei', Arial, sans-serif;
                  line-height: 1.6;
                  color: #333;
                  background-color: #f5f5f5;
                }}
                .download-section {{
                  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                  padding: 30px;
                  border-radius: 12px;
                  margin: 20px 0;
                  text-align: center;
                }}
                .download-title {{
                  color: white;
                  font-size: 20px;
                  font-weight: bold;
                  margin-bottom: 15px;
                }}
                .download-btn {{
                  display: inline-block;
                  padding: 15px 40px;
                  background-color: white;
                  color: #667eea;
                  text-decoration: none;
                  border-radius: 8px;
                  font-size: 16px;
                  font-weight: bold;
                  box-shadow: 0 4px 15px rgba(0,0,0,0.2);
                }}
                .info-box {{
                  background: white;
                  padding: 20px;
                  border-radius: 8px;
                  margin: 20px 0;
                  border-left: 4px solid #667eea;
                }}
              </style>
            </head>
            <body>
              <h2 style="color: #667eea;">📊 台股每日分析報告</h2>
              
              <p><strong>分析時間:</strong> {datetime_str}</p>
              <div class="info-box">
                <h3 style="color: #667eea; margin-top: 0;">📎 郵件附件</h3>
                <ul style="margin: 10px 0;">
                  <li style="margin: 8px 0;">✅ <strong>tse_analysis_result.xlsx</strong> - 上市股票分析報表（完整資料）</li>
                  <li style="margin: 8px 0;">✅ <strong>otc_analysis_result.xlsx</strong> - 上櫃股票分析報表（完整資料）</li>
                  <li style="margin: 8px 0;">✅ <strong>ALL_TSE.html</strong> - 上市股票彙整圖表（互動式）</li>
                  <li style="margin: 8px 0;">✅ <strong>ALL_OTC.html</strong> - 上櫃股票彙整圖表（互動式）</li>
                </ul>
                <p style="color: #666; font-size: 14px; margin-top: 15px;">
                  💡 Excel 使用 Excel 開啟，HTML 使用瀏覽器開啟查看互動圖表
                </p>
              </div>
              
              <div class="info-box">
                <h3 style="color: #667eea; margin-top: 0;">📈 分析內容</h3>
                <ul style="margin: 10px 0;">
                  <li>✅ 成交量異常分析（標準差檢測）</li>
                  <li>✅ 三大法人買賣超統計</li>
                  <li>✅ 漲跌幅排行榜</li>
                  <li>✅ 技術指標分析圖表</li>
                </ul>
              </div>
              
              <p style="margin-top: 30px; color: #666; font-size: 12px; text-align: center; padding-top: 20px; border-top: 1px solid #eee;">
                🤖 此報告由 GitHub Actions 自動產生<br>
                📅 生成時間: {datetime_str}
              </p>
            </body>
          </html>
          """
              
              msg.attach(MIMEText(body, 'html', 'utf-8'))
              
              # 附加檔案
              files_to_attach = [
                  'StockInfo/tse_analysis_result.xlsx',
                  'StockInfo/otc_analysis_result.xlsx',
                  'StockInfo/ALL_TSE.html',
                  'StockInfo/ALL_OTC.html'
              ]
              
              for filename in files_to_attach:
                  if os.path.exists(filename):
                      with open(filename, 'rb') as f:
                          part = MIMEBase('application', 'octet-stream')
                          part.set_payload(f.read())
                          encoders.encode_base64(part)
                          part.add_header(
                              'Content-Disposition',
                              f'attachment; filename= {os.path.basename(filename)}'
                          )
                          msg.attach(part)
                          print(f'✅ 附加檔案: {filename}')
                  else:
                      print(f'⚠️  檔案不存在: {filename}')
              
              # 發送郵件
              try:
                  server = smtplib.SMTP('smtp.gmail.com', 587)
                  server.starttls()
                  server.login(sender_email, sender_password)
                  server.send_message(msg)
                  server.quit()
                  print(f'✅ 郵件已成功發送到: {recipients_str}')
              except Exception as e:
                  print(f'❌ 郵件發送失敗: {str(e)}')
                  raise
          
          if __name__ == '__main__':
              send_email()
          EOF
          
          # 執行發送郵件
          python send_email.py
      
      - name: Commit stock data to repo
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          echo "📋 準備提交的檔案列表:"
          echo ""
          
          # 取得台灣時間日期
          DATE=$(TZ=Asia/Taipei date +'%Y%m%d')
          echo "📅 當前日期: ${DATE}"
          echo ""
          
          # 檢查檔案是否存在
          echo "🔍 檢查 StockInfo 資料夾的檔案:"
          ls -lh StockInfo/ || echo "⚠️  StockInfo 資料夾不存在"
          echo ""
          
          # 強制加入所有檔案（使用 --force 忽略 .gitignore）
          echo "📦 加入檔案到 Git staging area..."
          
          # 加入 CSV 原始資料
          git add -f StockTSEDaily/*.csv 2>/dev/null || true
          git add -f StockTSEShares/*.csv 2>/dev/null || true
          git add -f StockOTCDaily/*.csv 2>/dev/null || true
          git add -f StockOTCShares/*.csv 2>/dev/null || true
          
          # 加入 StockInfo 資料夾的所有檔案（包括帶日期的）
          echo "📦 加入 StockInfo 資料夾的檔案..."
          
          # 不帶日期的檔案
          git add -f StockInfo/tse_analysis_result.xlsx 2>/dev/null || true
          git add -f StockInfo/otc_analysis_result.xlsx 2>/dev/null || true
          git add -f StockInfo/tse_analysis_result_complete.html 2>/dev/null || true
          git add -f StockInfo/otc_analysis_result_complete.html 2>/dev/null || true
          git add -f StockInfo/ALL_TSE.html 2>/dev/null || true
          git add -f StockInfo/ALL_OTC.html 2>/dev/null || true
          git add -f StockInfo/TSE_buy_ranking.txt 2>/dev/null || true
          git add -f StockInfo/OTC_buy_ranking.txt 2>/dev/null || true
          
          # 帶日期的檔案
          git add -f StockInfo/tse_analysis_result_${DATE}.xlsx 2>/dev/null || true
          git add -f StockInfo/otc_analysis_result_${DATE}.xlsx 2>/dev/null || true
          git add -f StockInfo/ALL_TSE_${DATE}.html 2>/dev/null || true
          git add -f StockInfo/ALL_OTC_${DATE}.html 2>/dev/null || true
          
          # 也加入所有可能的帶日期檔案（萬用字元）
          git add -f StockInfo/*_[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9].xlsx 2>/dev/null || true
          git add -f StockInfo/*_[0-9][0-9][0-9][0-9][0-9][0-9][0-9][0-9].html 2>/dev/null || true
          
          echo ""
          echo "📦 Git staging area 中的檔案:"
          git diff --staged --name-status
          echo ""
          
          echo "🔍 檢查特定帶日期檔案是否存在:"
          [ -f "StockInfo/tse_analysis_result_${DATE}.xlsx" ] && echo "✅ tse_analysis_result_${DATE}.xlsx 存在" || echo "❌ tse_analysis_result_${DATE}.xlsx 不存在"
          [ -f "StockInfo/otc_analysis_result_${DATE}.xlsx" ] && echo "✅ otc_analysis_result_${DATE}.xlsx 存在" || echo "❌ otc_analysis_result_${DATE}.xlsx 不存在"
          [ -f "StockInfo/ALL_TSE_${DATE}.html" ] && echo "✅ ALL_TSE_${DATE}.html 存在" || echo "❌ ALL_TSE_${DATE}.html 不存在"
          [ -f "StockInfo/ALL_OTC_${DATE}.html" ] && echo "✅ ALL_OTC_${DATE}.html 存在" || echo "❌ ALL_OTC_${DATE}.html 不存在"
          echo ""
          
          # 檢查是否有變更
          if git diff --staged --quiet; then
            echo "❌ 沒有檔案變更，跳過提交"
            echo ""
            echo "🔍 除錯資訊："
            echo "StockInfo 目錄內容:"
            ls -la StockInfo/ 2>/dev/null || echo "StockInfo 不存在"
            echo ""
            echo "Git 狀態:"
            git status
          else
            echo "✅ 發現檔案變更，準備提交..."
            echo ""
            
            # 提交並推送
            git commit -m "📊 更新股票資料 $(TZ=Asia/Taipei date +'%Y-%m-%d %H:%M')"
            git push
            
            echo ""
            echo "✅ 成功推送資料到 repository"
            echo ""
            echo "📂 已更新的檔案:"
            git diff HEAD~1 --name-only | while read file; do
              echo "   ✓ $file"
            done
          fi
      
      - name: Clean old releases (keep last 30 days)
        uses: dev-drprasad/delete-older-releases@v0.3.2
        with:
          keep_latest: 7
          delete_tags: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}