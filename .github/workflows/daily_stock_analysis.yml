name: 每日台股分析

on:
  schedule:
    # 每天 UTC 09:00 執行 = 台灣時間下午 17:00 (UTC+8)
    - cron: '0 9 * * *'
  
  # 允許手動觸發測試
  workflow_dispatch:

jobs:
  stock-analysis:
    runs-on: ubuntu-latest
    
    # 給予 workflow 寫入權限以便 push
    permissions:
      contents: write
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            fonts-noto-cjk \
            chromium-browser \
            chromium-chromedriver
      
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Ensure data directories exist
        run: |
          # 這些資料夾已經在 repo 中，這裡只確保臨時目錄存在
          mkdir -p StockHTML
          mkdir -p StockPNG
          mkdir -p StockOTCHTML
          mkdir -p StockOTCPNG
      
      - name: Run stock analysis
        env:
          TZ: 'Asia/Taipei'
        run: |
          python 上市上櫃全流程.py
      
      - name: Commit and push stock data
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          # 加入所有 CSV 資料檔案（原始資料 + 歷史資料）- 永久保存
          git add StockDaily/*.csv \
                  StockShares/*.csv \
                  StockOTCDaily/*.csv \
                  StockOTCShares/*.csv \
                  StockHistory/*.csv \
                  StockOTCHistory/*.csv
          
          # 檢查是否有變更
          if git diff --staged --quiet; then
            echo "沒有資料變更，跳過 commit"
          else
            # 顯示變更的檔案
            echo "📝 變更的檔案："
            git diff --staged --name-only
            
            git commit -m "📊 更新股票資料 $(TZ=Asia/Taipei date +'%Y-%m-%d %H:%M')"
            git push
            echo "✅ 成功推送資料到 repository"
          fi
      
      - name: Upload analysis results (報表與圖表)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: stock-analysis-results-${{ github.run_number }}
          path: |
            StockHTML/*.html
            StockPNG/*.png
            StockOTCHTML/*.html
            StockOTCPNG/*.png
            *.xlsx
          retention-days: 30
      
      - name: Upload latest reports (最新報表)
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: latest-stock-reports
          path: |
            *.xlsx
            StockHTML/*.html
            StockOTCHTML/*.html
          retention-days: 7